import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Wheel-of-Golf-React',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
